---
- hosts: managers
  gather_facts: true
  roles:
    - kubelet
    - kubeadm
    - calico
    - kubectl
    - helm
  environment: "{{env}}"
- hosts: workers
  gather_facts: true
  roles:
    - kubelet
  environment: "{{env}}"

